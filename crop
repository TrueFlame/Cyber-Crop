class crop(crop_id, type, name, size, est_yield):
    self.crop_id = crop_id
    seld.type = type
    self.name = name
    self.size = size
    self.est_yield = est_yield
    
    subject_a = crop(1, tomato, 90, 1.09)
